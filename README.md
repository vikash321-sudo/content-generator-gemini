# Bizconvert – Content Generator (Gemini)

Generate on-brand content using Google Gemini and auto-log to Google Sheets.

## Demo (local)
```bash
pip install -r requirements.txt
streamlit run app.py
